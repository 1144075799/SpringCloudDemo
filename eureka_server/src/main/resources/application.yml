server:
  port: 9000  #程序启动后的端口，也就是Tomcat的端口，我们也可以自己定义
# 因为当前的eureka是单机的，所以我们需要做一些配置 配置单机模式
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:9000/eureka
